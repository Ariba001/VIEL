[project]
name = "your-repo"
version = "0.1.0"
requires-python = ">=3.10,<3.12"
dependencies = [
    "angr",
    "capstone",
    "lief",
    "networkx",
    "numpy",
    "pandas",
    "scikit-learn",
    "matplotlib",
    "pyyaml",
]
